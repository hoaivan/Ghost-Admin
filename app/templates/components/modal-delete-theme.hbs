<header class="modal-header" data-test-delete-theme-modal>
    <h1>Are you sure you want to delete this</h1>
</header>
<a class="close" href="" title={{t "Close"}} {{action "closeModal"}}>{{svg-jar "close"}}<span class="hidden">{{t "Close"}}</span></a>

<div class="modal-body">
    <p>{{t "You're about to delete"}} "<strong>{{theme.label}}</strong>". {{t "This is permanent! We warned you, k?"}} {{t "Maybe"}} <a href="#" {{action download}}>{{t "Download your theme before continuing"}}</a></p>
</div>

<div class="modal-footer">
    <button {{action "closeModal"}} class="gh-btn" data-test-cancel-button><span>{{t "Cancel"}}</span></button>
    {{gh-task-button "Delete" successText="Deleted" task=deleteTheme class="gh-btn gh-btn-red gh-btn-icon" data-test-delete-button=true}}
</div>
